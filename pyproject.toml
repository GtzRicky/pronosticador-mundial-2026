[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quiniela-mundial-2026"
version = "0.1.0"
description = "Scaffold local-first para predicciones de marcador exacto del Mundial 2026"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
